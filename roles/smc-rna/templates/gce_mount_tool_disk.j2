#!/bin/bash

sudo /usr/share/google/safe_format_and_mount -m "mkfs.ext4 -F" /dev/sdb /opt/tools

grep "/opt/tools" /etc/fstab
if [ $? != "0" ]; then
  sudo bash -c 'echo -e "/dev/sdb\t/opt/tools\text4\tdefaults\t0\t2" >> /etc/fstab'
fi
sudo chown -R ubuntu:ubuntu /opt/tools

sudo supervisorctl restart galaxy: