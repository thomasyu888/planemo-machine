
- name: setuptools
  shell: "easy_install -U setuptools"

- name: cwl tools
  pip: name=cwl-runner

- name: cwltools repo
  git: repo=git://github.com/bmeg/cwltool.git dest={{ smc_cwl_path }} accept_hostkey=yes version=cache-intermediate-output

- name: cwltools install
  shell: "python {{ smc_cwl_path }}/setup.py install"
  sudo: True
  sudo_user: "{{ galaxy_user_name }}"

- name: cwlrunner install
  shell: "python {{ smc_cwl_path }}/cwl-runner/setup.py install"
  sudo: True
  sudo_user: "{{ galaxy_user_name }}"

- name: rabix
  pip: name=rabix

